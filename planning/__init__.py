"""Lesson planning: where a lesson comes from, and how a unit is sequenced.

Separate from `curriculum/`, which holds what the National Curriculum says, and
from `generators/`, which turns content into documents. This package holds the
decisions about *planning* -- which scheme governs a subject, whether its
sequence may be changed, and how many lessons a unit should run to.
"""
